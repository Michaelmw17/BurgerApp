<li>
  {{this.burger_name}}

  <button class="devoured-btn" data-id="{{this.id}}" data-devoured="{{this.devoured}}">
    {{#if devoured}}<i class="fas fa-hamburger"></i>{{else}}{{/if}}
  </button>

</li>